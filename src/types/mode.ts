export enum Mode {
  /** Only events from the selected chapter are shuffled */
  Single = 'single',
  /** All previous chapters and the selected chapter are shuffled */
  Summary = 'summary',
  /** All events from all chapters are shuffled */
  ForFun = 'forFun',
}
